{
  "name": "arcade-playground",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "sql": "bun run --env-file=../.env sql.ts",
    "rag": "bun run --env-file=../.env rag.ts",
    "parse": "bun run --env-file=../.env parse.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@arcadeai/arcadejs": "^1.2.1"
  }
}
